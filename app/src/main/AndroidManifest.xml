<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="vinasource.com.strategydemo">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".activity.LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>


        <activity android:name=".modules.registration.RegistrationFlowActivity"/>
        <activity android:name=".basemodules.colleges.RegisterUserNameCollegesBaseActivity"/>
        <activity android:name=".basemodules.colleges.VerifyEmailActivity"/>


    </application>

</manifest>